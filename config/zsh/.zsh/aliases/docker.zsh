alias docker="sudo docker"
alias dr="docker"

alias dps="docker ps"
alias dpa="docker ps -a"
alias dri="docker images"

alias drpl="docker pull"
alias drpu="docker push"

alias drun="docker run -it"
alias dexec="docker exec -it"

alias drm="docker rm"
alias drmi="docker rmi"

alias dco="docker compose"

alias dr-kali="docker start mykali && docker attach mykali"
alias drparrot="docker run --rm -ti --network host -v $PWD/work:/work parrotsec/security"
